/**
 * La clase App es el punto de entrada principal de la aplicación.
 * Contiene el método main que inicia la ejecución del programa.
 */
public class App {

    /**
     * El método principal de la aplicación.
     * <p>
     * Este método se encarga de ejecutar el método main de la clase TestG.
     * La línea que ejecuta TestMinPriorityQueue.main(args) está comentada,
     * pero puede ser descomentada para ejecutar ese método en su lugar.
     * </p>
     *
     * @param args Los argumentos de línea de comandos.
     * @throws Exception si ocurre un error durante la ejecución de los métodos invocados.
     */
    public static void main(String[] args) throws Exception {
        TestG.main(args);
        // TestMinPriorityQueue.main(args);
    }
}
